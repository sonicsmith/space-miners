pragma solidity ^0.5.2;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Kerium.sol";

contract TestKerium {

  Kerium public kerium;
  uint public initialBalance = 1000 finney;
/*
  function beforeEach() public {
    kerium = new Kerium();
  }

  function testItIsDetailedCorrectly() public {
    Assert.equal(kerium.name(), "Kerium", "It has the correct name");

  }

  function testItMints() public {
    Assert.equal(kerium.totalSupply(), 0, "Total supply should start at 0.");
    kerium.mint(address(this), 1);
    Assert.equal(kerium.totalSupply(), 1, "Minting should increase the total supply.");
  }

  function() external payable {}
*/
}
